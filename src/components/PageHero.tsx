import Link from "next/link";
export default function PageHero({kicker,title,accent,description,primary,secondary}:{kicker:string;title:string;accent?:string;description:string;primary?:{label:string;href:string};secondary?:{label:string;href:string}}){return <header className="page-hero"><div className="wrap"><span className="page-kicker">// {kicker}</span><h1>{title}{accent&&<> <span className="accent-text">{accent}</span></>}</h1><p>{description}</p>{(primary||secondary)&&<div className="hero-actions">{primary&&<Link className="btn-primary" href={primary.href}>{primary.label}</Link>}{secondary&&<Link className="btn-ghost" href={secondary.href}>{secondary.label}</Link>}</div>}</div></header>}
