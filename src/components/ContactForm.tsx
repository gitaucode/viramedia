"use client";
import { FormEvent, useState } from "react";
export default function ContactForm(){const [status,setStatus]=useState<'idle'|'sending'|'sent'|'error'|'config'>('idle');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;setStatus('sending');const fd=new FormData(form);const data=Object.fromEntries(fd.entries());try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(r.ok){setStatus('sent');form.reset()}else if(j.code==='EMAIL_NOT_CONFIGURED')setStatus('config');else setStatus('error')}catch{setStatus('error')}}return <form className="contact-form" onSubmit={submit}><h2>Send the brief.</h2><p>A few useful details are enough. We can shape the scope from there.</p><div className="field-grid"><div className="field"><label htmlFor="name">Your name *</label><input id="name" name="name" required maxLength={80}/></div><div className="field"><label htmlFor="company">Company / brand *</label><input id="company" name="company" required maxLength={120}/></div><div className="field"><label htmlFor="email">Email *</label><input id="email" name="email" type="email" required maxLength={160}/></div><div className="field"><label htmlFor="phone">Phone / WhatsApp</label><input id="phone" name="phone" maxLength={40}/></div><div className="field"><label htmlFor="service">What do you need?</label><select id="service" name="service"><option>Short-form content production</option><option>Creator campaign</option><option>UGC production</option><option>Paid amplification</option><option>Ongoing TikTok / Reels support</option><option>Not sure yet</option></select></div><div className="field"><label htmlFor="budget">Indicative budget</label><select id="budget" name="budget"><option>Not decided</option><option>Under KSh 80K</option><option>KSh 80K–150K</option><option>KSh 150K–300K</option><option>KSh 300K+</option></select></div><div className="field full"><label htmlFor="message">What are you trying to achieve? *</label><textarea id="message" name="message" required maxLength={2000} placeholder="Tell us what you sell, who you want to reach and what you want the campaign to do."/></div><div className="honeypot" aria-hidden="true"><label htmlFor="website">Website</label><input id="website" name="website" tabIndex={-1} autoComplete="off"/></div></div><div className="form-actions"><span>{status==='sent'?'Brief sent. We have it.':''}</span><button className="form-button" disabled={status==='sending'}>{status==='sending'?'Sending…':'Send brief →'}</button></div>{status==='error'&&<p className="form-error">Something went wrong sending the form. Please email us directly instead.</p>}{status==='config'&&<p className="form-error">Online form delivery is not configured yet. Please use the email link on this page.</p>}</form>}
