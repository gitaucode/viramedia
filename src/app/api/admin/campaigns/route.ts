import { NextResponse } from "next/server";
import { isAdminAuthenticated } from "@/lib/admin-auth";
import { createCampaign, listCampaigns, updateCampaign } from "@/lib/ops-db";
const allowed=new Set(['planning','active','paused','complete','cancelled']);
export async function GET(){if(!(await isAdminAuthenticated()))return NextResponse.json({error:'Unauthorized'},{status:401});const campaigns=await listCampaigns();if(!campaigns)return NextResponse.json({error:'Database is not configured',code:'DB_NOT_CONFIGURED'},{status:503});return NextResponse.json({campaigns})}
export async function POST(request:Request){if(!(await isAdminAuthenticated()))return NextResponse.json({error:'Unauthorized'},{status:401});try{const b=await request.json();if(typeof b.name!=='string'||!b.name.trim())return NextResponse.json({error:'Campaign name required'},{status:400});if(b.status&&!allowed.has(b.status))return NextResponse.json({error:'Invalid status'},{status:400});await createCampaign(b);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Invalid request'},{status:400})}}
export async function PATCH(request:Request){if(!(await isAdminAuthenticated()))return NextResponse.json({error:'Unauthorized'},{status:401});try{const b=await request.json();const id=Number(b.id);if(!Number.isInteger(id)||id<1)return NextResponse.json({error:'Invalid campaign'},{status:400});if(b.status&&!allowed.has(b.status))return NextResponse.json({error:'Invalid status'},{status:400});await updateCampaign(id,b);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Invalid request'},{status:400})}}
