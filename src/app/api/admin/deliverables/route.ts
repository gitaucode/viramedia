import { NextResponse } from "next/server";
import { isAdminAuthenticated } from "@/lib/admin-auth";
import { createDeliverable, listDeliverables, updateDeliverable } from "@/lib/ops-db";
const allowed=new Set(['pending','in_progress','submitted','approved','done']);
export async function GET(request:Request){if(!(await isAdminAuthenticated()))return NextResponse.json({error:'Unauthorized'},{status:401});const u=new URL(request.url);const id=Number(u.searchParams.get('campaignId')||0);const deliverables=await listDeliverables(id||undefined);if(!deliverables)return NextResponse.json({error:'Database is not configured',code:'DB_NOT_CONFIGURED'},{status:503});return NextResponse.json({deliverables})}
export async function POST(request:Request){if(!(await isAdminAuthenticated()))return NextResponse.json({error:'Unauthorized'},{status:401});try{const b=await request.json();if(!Number.isInteger(Number(b.campaignId))||!String(b.title||'').trim())return NextResponse.json({error:'Campaign and title required'},{status:400});if(b.status&&!allowed.has(b.status))return NextResponse.json({error:'Invalid status'},{status:400});await createDeliverable(b);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Invalid request'},{status:400})}}
export async function PATCH(request:Request){if(!(await isAdminAuthenticated()))return NextResponse.json({error:'Unauthorized'},{status:401});try{const b=await request.json();const id=Number(b.id);if(!Number.isInteger(id)||id<1)return NextResponse.json({error:'Invalid deliverable'},{status:400});if(b.status&&!allowed.has(b.status))return NextResponse.json({error:'Invalid status'},{status:400});await updateDeliverable(id,b);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Invalid request'},{status:400})}}
