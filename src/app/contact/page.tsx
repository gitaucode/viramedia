import type { Metadata } from "next";import PageHero from "@/components/PageHero";import ContactForm from "@/components/ContactForm";import { contact } from "@/data/site";
export const metadata:Metadata={title:"Contact",description:"Send Vira Media a brand brief for short-form content or creator campaigns."};
export default function Contact(){const wa=`https://wa.me/${contact.whatsapp}?text=${encodeURIComponent("Hi Vira, I'd like to discuss a campaign for my brand.")}`;return <><PageHero kicker="Start a Project" title="Send the brief." accent="We shape the rest." description="You do not need a finished campaign deck. Tell us the brand, audience, goal and rough scope and we can start from there."/><section className="page-section"><div className="wrap contact-layout"><aside><div className="contact-card"><h3>Prefer direct?</h3><a className="contact-link" href={`mailto:${contact.email}`}>{contact.email}</a><a className="contact-link" href={wa} target="_blank" rel="noopener noreferrer">WhatsApp Vira ↗</a><a className="contact-link" href={`tel:${contact.phone}`}>{contact.phone}</a></div><div className="contact-card" style={{marginTop:14}}><h3>Useful in the first brief</h3><p className="body-copy">What you sell, who you want to reach, what you want the campaign to achieve, where the content will run and any timing or budget constraints.</p></div></aside><ContactForm/></div></section></>}
